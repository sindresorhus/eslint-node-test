import {getTester, parsers} from './utils/test.js';

const {test} = getTester(import.meta);

const withImport = code => `import test from 'node:test';\nimport assert from 'node:assert/strict';\n${code}`;
const withTimerNamespaceImport = code => `import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport * as timers from 'node:timers';\n${code}`;
const withNamedTimerImport = code => withImport(`import {setTimeout as delay} from 'node:timers';\n${code}`);
const inTest = code => withImport(`test('example', () => {\n\t${code}\n});`);
const inAsyncTest = code => withImport(`test('example', async () => {\n\t${code}\n});`);

test.snapshot({
	valid: [
		'import assert from \'node:assert/strict\';\nsetTimeout(() => assert.ok(value));',
		inTest('setTimeout(() => handle(value));'),
		inTest('setImmediate(externalCallback);'),
		withImport('test(\'example\', function * () { setTimeout(() => assert.ok(value)); });'),
		withImport('test(\'example\', (t = setTimeout(() => assert.ok(value))) => {});'),
		withImport('test(\'outer\', function * () { test(\'inner\', () => { setTimeout(() => assert.ok(value)); }); });'),
		withImport('test(\'outer\', (t = test(\'inner\', () => { setTimeout(() => assert.ok(value)); })) => {});'),
		withImport('test(\'outer\', () => { function helper() { test(\'inner\', () => { setTimeout(() => assert.ok(value)); }); } });'),
		withImport('test(\'outer\', () => { class Example { value = test(\'inner\', () => { setTimeout(() => assert.ok(value)); }); } });'),
		inTest('setTimeout(function * () { assert.ok(value); });'),
		inTest('load().then(async function * () { assert.ok(value); });'),
		inTest('setTimeout();'),
		inTest('queueMicrotask(() => { const check = () => assert.ok(value); check(); });'),
		inAsyncTest('await load().then(value => assert.ok(value));'),
		inTest('return load().catch(error => assert.ifError(error));'),
		inTest('const promise = load().finally(() => assert.ok(cleanedUp));'),
		inTest('const promise = condition && load().then(() => assert.ok(value));'),
		inTest('return condition ? load().then(() => assert.ok(value)) : undefined;'),
		inAsyncTest('await new Promise(resolve => { setTimeout(() => { assert.ok(value); resolve(); }, 10); });'),
		inTest('return new Promise(resolve => { setImmediate(() => { assert.ok(value); resolve(); }); });'),
		inTest('const promise = new Promise(resolve => { queueMicrotask(() => { assert.ok(value); resolve(); }); });'),
		inTest('return new Promise(resolve => { setTimeout(() => { assert.ok(value); resolve(); }); }).then(() => {});'),
		withImport('test(\'example\', t => {\n\tt.plan(1, {wait: 100});\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
		withImport('test(\'example\', t => {\n\tconst shouldWait = true;\n\tt.plan(1, {wait: shouldWait});\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
		withImport('test(\'example\', t => {\n\tfunction helper() {}\n\tt.plan(1, {wait: true});\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
		{
			code: withImport('test(\'example\', t => {\n\tt.plan(1, ({wait: true} as const));\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
			languageOptions: {parser: parsers.typescript},
		},
		withImport('test(\'example\', t => {\n\tt.plan(1, {wait: 100});\n\tload().then(() => t.assert.ok(value));\n});'),
		withImport('test(\'example\', t => {\n\tt.plan(1, {wait: true});\n\tqueueMicrotask(() => t.test(\'child\', () => {}));\n});'),
		inTest('function setTimeout(callback) { callback(); }\n\tsetTimeout(() => assert.ok(value));'),
		inTest('const queueMicrotask = callback => callback();\n\tqueueMicrotask(() => { throw error; });'),
		withImport('describe(\'suite\', () => { setTimeout(() => assert.ok(value)); });'),
		inTest('timers.setTimeout(() => assert.ok(value));'),
		inTest('promise[method](() => assert.ok(value));'),
		inTest('new Promise(resolve => { function schedule() { setTimeout(() => assert.ok(value)); } });'),
		withImport('test(\'example\', (t, done) => { setTimeout(() => { assert.ok(value); done(); }); });'),
		withImport('test(\'example\', (t, done) => { setTimeout(() => assert.ok(value)); done(); });'),
		'import {beforeEach} from \'node:test\';\nimport assert from \'node:assert/strict\';\nbeforeEach((t, done) => { setImmediate(() => { assert.ok(value); done(); }); });',
		inTest('setTimeout(() => { try { throw error; } catch {} });'),
		inTest('load().then(() => { try { throw error; } catch {} });'),
		inTest('setTimeout(() => { try { assert.ok(value); } catch {} });'),
		inTest('load().then(() => { try { assert.ok(value); } catch {} });'),
		inTest('load().then(() => assert.fail()).catch(() => {});'),
		inTest('load().then(() => assert.fail()).catch(handleError);'),
		inTest('load().then(() => { throw error; }).then(undefined, () => {});'),
		inTest('setTimeout(() => { assert.rejects(load()).catch(() => {}); });'),
		inTest('load().then(() => { assert.rejects(other()).catch(() => {}); });'),
		inTest('setTimeout(async () => { try { await assert.rejects(load()); } catch {} });'),
		inTest('class Example { value = setTimeout(() => assert.ok(value)); }'),
		inTest('class Example { value = load().then(() => assert.ok(value)); }'),
		inTest('setTimeout(() => { class Example { value = assert.ok(value); } });'),
		inTest('load().then(() => { class Example { value = assert.ok(value); } });'),
		{
			code: inTest('class Example { accessor value = setTimeout(() => assert.ok(value)); }'),
			languageOptions: {parser: parsers.typescript},
		},
		withNamedTimerImport('test(\'example\', delay => { delay(() => assert.ok(value)); });'),
		withTimerNamespaceImport('test(\'example\', timers => { timers.setTimeout(() => assert.ok(value)); });'),
		'import {afterEach} from \'node:test\';\nimport assert from \'node:assert/strict\';\nafterEach(() => { load().then(() => assert.ok(value)); });',
		inAsyncTest('load().then(value => assert.ok(value));'),
		inAsyncTest('load()?.then(value => assert.ok(value));'),
		inTest('(load().then(() => assert.ok(value)), cleanup());'),
		inTest('(cleanup(), load().then(() => assert.ok(value)));'),
		inTest('void (cleanup(), load().then(() => assert.ok(value)));'),
		inTest('for (load().then(() => assert.ok(value)); condition; update()) {}'),
		inTest('for (; condition; load().then(() => assert.ok(value))) {}'),
		inTest('condition && load().then(() => assert.ok(value));'),
		inTest('condition ? load().then(() => assert.ok(value)) : cleanup();'),
		inTest('return load().then(() => assert.ok(value)) && other;'),
		inTest('return load().then(() => assert.ok(value)) ? first : second;'),
		inTest('load().then(() => assert.fail()).catch();'),
		inTest('load().then(() => assert.fail()).catch({});'),
		inTest('load().then(() => assert.fail()).catch(false && handler);'),
		inTest('load().then(() => { assert.rejects(other()); }).catch(() => {});'),
		inTest('load().finally((unused = assert.ok(value)) => {});'),
		inTest('load().then(function * (unused = assert.ok(value)) {});'),
		withImport('test(\'example\', async (t, unused = void load().then(() => assert.ok(value))) => {});'),
		inAsyncTest('return void load().then(() => assert.ok(value));'),
		inTest('load().then(() => { assert.ok(first); assert.ok(second); });'),
		{
			code: inAsyncTest('(load().then(value => assert.ok(value)) as Promise<void>);'),
			languageOptions: {parser: parsers.typescript},
		},
	],
	invalid: [
		inTest('setTimeout(() => assert.ok(value), 10);'),
		inTest('setImmediate(() => { throw new Error(\'late\'); });'),
		inTest('queueMicrotask(() => assert.equal(value, 1));'),
		withImport('test(\'parent\', t => {\n\tsetTimeout(() => t.test(\'child\', () => {}), 10);\n});'),
		withImport('test(\'parent\', t => {\n\tsetTimeout(() => { t.test(\'child\', () => {}); }, 10);\n});'),
		withImport('test(\'example\', t => {\n\tsetImmediate(() => t.assert.ok(value));\n});'),
		withImport('test(\'example\', t => {\n\tt.plan(1, {wait: false});\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
		withImport('test(\'example\', t => {\n\tconst shouldWait = false;\n\tt.plan(1, {wait: shouldWait});\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
		withImport('test(\'example\', t => {\n\tt.plan(1, {wait: shouldWait});\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
		withImport('test(\'example\', t => {\n\tt.plan(1, {wait: true, wait: false});\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
		withImport('test(\'example\', t => {\n\tconst options = {wait: false};\n\tt.plan(1, {wait: true, ...options});\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
		withImport('test(\'example\', t => {\n\tif (false) { t.plan(1, {wait: true}); }\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
		withImport('test(\'example\', t => {\n\tif (shouldWait) { t.plan(1, {wait: true}); }\n\tsetTimeout(() => t.assert.ok(value), 10);\n});'),
		withImport('test(\'example\', t => {\n\tsetTimeout(() => t.assert.ok(value), 10);\n\treturn;\n\tt.plan(1, {wait: true});\n});'),
		'import {beforeEach} from \'node:test\';\nimport assert from \'node:assert/strict\';\nbeforeEach(() => { setTimeout(() => assert.ok(value), 10); });',
		'import test from \'node:test\';\nimport assert from \'node:assert/strict\';\ntest.beforeEach(() => { setTimeout(() => assert.ok(value), 10); });',
		'import * as nodeTest from \'node:test\';\nimport assert from \'node:assert/strict\';\nnodeTest.afterEach(() => { setImmediate(() => assert.ok(value)); });',
		withNamedTimerImport('test(\'example\', () => { delay(() => assert.ok(value), 10); });'),
		withTimerNamespaceImport('test(\'example\', () => { timers.setImmediate(() => { throw error; }); });'),
		'import test from \'node:test\';\nimport assert from \'node:assert/strict\';\nimport {setTimeout as delay} from \'timers\';\ntest(\'example\', () => { delay(() => assert.ok(value), 10); });',
		'import test from \'node:test\';\nimport {strictEqual as equal} from \'node:assert/strict\';\ntest(\'example\', () => { setTimeout(() => equal(value, 1)); });',
		'import test from \'node:test\';\nimport assert from \'node:assert\';\ntest(\'example\', () => { setTimeout(() => assert.strict.equal(actual, expected)); });',
		inTest('load().catch(error => { throw error; });'),
		inTest('load().then(() => assert.fail()).catch(() => { throw error; });'),
		withImport('test(\'example\', t => { setTimeout((unused = t.assert.ok(value)) => {}, 10); });'),
		withImport('test(\'example\', (t = fallback) => { setTimeout(() => t.assert.ok(value)); });'),
		withImport('test(\'example\', function * (t, unused = setTimeout(() => assert.ok(value))) {});'),
		inTest('setTimeout(function * (unused = assert.ok(value)) {});'),
		inTest('(new Promise(resolve => { setTimeout(() => { assert.ok(value); resolve(); }); }), cleanup());'),
		inTest('void (cleanup(), new Promise(resolve => { setTimeout(() => { assert.ok(value); resolve(); }); }));'),
		inTest('condition && new Promise(resolve => { setTimeout(() => { assert.ok(value); resolve(); }); });'),
		inTest('new Promise(resolve => { setTimeout(() => { assert.ok(value); resolve(); }); }).then(() => {});'),
		inAsyncTest('await new Promise(resolve => resolve(), setTimeout(() => assert.ok(value)));'),
		inTest('class Example { static value = setTimeout(() => assert.ok(value)); }'),
		inTest('class Example { [setTimeout(() => assert.ok(value))] = value; }'),
		inTest('setTimeout(() => { class Example { static value = assert.ok(value); } });'),
		inTest('setTimeout(() => { try { assert.rejects(load()); } catch {} });'),
		inTest('new Promise(resolve => { setTimeout(() => { assert.ok(value); resolve(); }); });'),
		inTest('void new Promise(resolve => { setTimeout(() => { assert.ok(value); resolve(); }); });'),
		withImport('test(\'parent\', t => {\n\tload().finally(() => t.test(\'child\', () => {}));\n});'),
		inTest('setTimeout(() => { assert.ok(first); assert.ok(second); });'),
		withImport('test(\'parent\', t => { setTimeout(() => assert.ok(t.test(\'child\', () => {}))); });'),
		withImport('test(\'parent\', t => { setTimeout(() => t.test(\'child\', assert.ok(value))); });'),
		withImport('test(\'parent\', async t => { await t.test(\'child\', childContext => { setTimeout(() => childContext.assert.ok(value)); }); });'),
		{
			code: inTest('setTimeout((() => assert.ok(value)) as () => void);'),
			languageOptions: {parser: parsers.typescript},
		},
		{
			code: inTest('(setTimeout as typeof setTimeout)(() => assert.ok(value));'),
			languageOptions: {parser: parsers.typescript},
		},
		{
			code: withTimerNamespaceImport('test(\'example\', () => { (timers.setImmediate as typeof timers.setImmediate)(() => { throw error; }); });'),
			languageOptions: {parser: parsers.typescript},
		},
		{
			code: withTimerNamespaceImport('test(\'example\', () => { (timers as typeof timers).setTimeout(() => assert.ok(value)); });'),
			languageOptions: {parser: parsers.typescript},
		},
		{
			code: inTest('setTimeout(() => (assert as typeof assert).ok(value));'),
			languageOptions: {parser: parsers.typescript},
		},
		{
			code: withImport('test(\'example\', t => { setTimeout(() => (t as typeof t).assert.ok(value)); });'),
			languageOptions: {parser: parsers.typescript},
		},
		{
			code: 'import test from \'node:test\';\nimport {strictEqual as equal} from \'node:assert/strict\';\ntest(\'example\', () => { setTimeout(() => (equal as typeof equal)(value, 1)); });',
			languageOptions: {parser: parsers.typescript},
		},
	],
});
